# Staging environment defaults (non-secret — committed to git)
BASE_URL=https://staging.smp.example.com
API_BASE_URL=https://api.staging.smp.example.com

ADMIN_EMAIL=admin@staging.example.com
ADMIN_PASSWORD=staging-password

STANDARD_USER_EMAIL=user@staging.example.com
STANDARD_USER_PASSWORD=staging-password

API_KEY=staging-api-key
